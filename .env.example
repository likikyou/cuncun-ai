# ============================================
# My Echo 配置文件
# ============================================
# 请复制本文件为 .env 并填入您的配置值
# 注意：不要将 .env 文件提交到代码仓库
# ============================================

# ============================================
# 1. 飞书平台配置（必需）
# ============================================

# 飞书应用的唯一标识符
# 获取方式：登录飞书开放平台 -> 应用管理 -> 选择应用 -> 基本信息 -> App ID
FEISHU_APP_ID=your_feishu_app_id_here

# 飞书应用密钥
# 获取方式：登录飞书开放平台 -> 应用管理 -> 选择应用 -> 凭证与基础信息 -> App Secret
FEISHU_APP_SECRET=your_feishu_app_secret_here

# 飞书验证令牌
# 获取方式：登录飞书开放平台 -> 应用管理 -> 选择应用 -> 事件订阅 -> 验证令牌
# 说明：用于验证飞书服务器的回调请求，必须与飞书平台配置一致
FEISHU_VERIFY_TOKEN=your_verify_token_here

# 飞书加密密钥（可选，建议启用）
# 获取方式：登录飞书开放平台 -> 应用管理 -> 选择应用 -> 事件订阅 -> 启用加密
# 说明：用于解密飞书发送的加密消息，如果不配置将跳过解密步骤
FEISHU_ENCRYPT_KEY=your_encrypt_key_here

# ============================================
# 2. AI 模型配置（必需）
# ============================================

# DeepSeek API 密钥
# 获取方式：访问 https://platform.deepseek.com/ -> 注册登录 -> API 管理 -> 创建 API Key
# 说明：用于调用 DeepSeek 大语言模型进行对话生成
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# 硅基流动 API 密钥（可选）
# 获取方式：访问 https://www.siliconflow.cn/ -> 注册登录 -> API 管理 -> 获取 API Key
# 说明：用于向量化和 Embedding 计算，如果不需要语音匹配功能可以不配置
SILICONFLOW_API_KEY=your_siliconflow_api_key_here

# ============================================
# 3. 路径配置（可选）
# ============================================
# 以下配置使用默认值，如果需要自定义路径可以修改

# 对话历史数据库路径
# 说明：SQLite 数据库文件，用于存储与用户的对话记录
# 默认值：./AI_banlu_cuncun_memory.db
# DB_PATH=./AI_banlu_cuncun_memory.db

# 提示词模板文件路径
# 说明：AI 角色的性格设定和行为规范
# 默认值：./prompt_template.txt
# PROMPT_PATH=./prompt_template.txt

# 语音资产向量库路径
# 说明：存储语音文件向量索引的 ChromaDB 数据库
# 默认值：./音频数据/cuncun_assets_db
# ASSETS_PATH=./音频数据/cuncun_assets_db

# 语音库文件目录路径
# 说明：存放原始音频文件的目录（OPUS 格式）
# 默认值：./音频数据/CunCun_Opus_Library
# VOICE_LIB=./音频数据/CunCun_Opus_Library

# 记忆向量库路径
# 说明：存储用户记忆向量索引的 ChromaDB 数据库
# 默认值：./cuncun_memory_db
# MEMORY_PATH=./cuncun_memory_db

# ============================================
# 4. 运维配置（可选）
# ============================================

# 日志文件路径
# 说明：JSON 格式结构化日志文件
# 默认值：./logs/feishu-cuncun.log
# LOG_FILE=./logs/feishu-cuncun.log

# 数据库备份目录
# 说明：自动备份文件存储位置
# 默认值：./backups
# BACKUP_DIR=./backups

# 管理员 Open ID（可选）
# 说明：用于接收系统告警通知的管理员飞书用户 ID
# 获取方式：向机器人发送消息，查看服务器日志中的 open_id
# ADMIN_OPEN_ID=your_admin_open_id_here

# 服务端口
# 说明：Flask 应用监听的端口号
# 默认值：8081
# PORT=8081
